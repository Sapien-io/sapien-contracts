Q2) In OriginationLib, function fundProject(), should the originationFee for the adapter be calculated using the amount pre or post protocol fee?
Q3) In OriginationLib, function fundProject(), originator can call function multiple times - is this intentional? In particular, $.originationAdapter[projectId] = adapter gets overwritten each time with potentially different adapter addresses, leaving a previously set adapter no longer reachable via getOriginationAdapter() as it only fetches the current one. Is it intended?
Q4) In ReputationLib, function update(), when we emit the event ReputationUpdated, should oldScore be the score pre or post decay?
Q5) The behavior for vault’s operations is rather consistent, it reverts if there is not exactly the amount, instead of taking what remains, even if it is less than what is requested. Just wanted to double-check that it is the intended behavior to revert if not enough is available. So far, I see it as a potential DoS attack vector if an attacker can create off-by-one amounts.
Q6) Project objects have an attribute called “minStakeToClaim”. The name suggests that contributors could optionally stake more to claim. However, in ContributionLib, function claimToContribute(), they cannot stake more than “minStakeToClaim”. Is it intended?
Q7) Should an originator allowed to be a validator for his own project?
Q8) Trying to evaluate the risk of front-running operations:
8a) Is the value of projectID deterministically calculated off-chain for a given originator, such that it is possible for another actor to prevent the originator to submit a given project, by creating a project with that deterministically generated projectID?
8b) Validators assigned to confirm the same contribution can replay the same commit and reveal data. Is it intended?
8c) A dispute can be front-runned by someone else with the same values, is it intended?
8d) Reporting an originator can be front-runned by someone else with the same values, is it intended?
Q9) Validators get an updated reputation before dispute phase. Since reputation updates happen with decay and can also be impacted by other updates for other projects, rolling back these updates when the dispute happens may not exactly give the same value. Is it intended?
Q10) Is this scenario valid? A misbehaving originator can misbehave at a discount, by leveraging the fact that the misbehavior can only be reported once. He creates a fake address, locks a bond, and calls reportOriginator() with a dummy evidenceHash. In that case, the Operator will reject the dispute because of the dummy evidenceHash, the originator will only lose bondAmount, and cannot be reported anymore for that project.
Q11) In ValidationLib, function computeConsensus(), contributors get a reputation bonus if their contribution is validated. However, in the case of a dispute of that validation that would be upheld, the reputation of the contributor would be reduced, but without considering the bonus. Is it intended?